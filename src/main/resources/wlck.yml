public:
    gab_wlck_ggxx:
        cell_phone: "#{phoneNumber.cellPhone}"
        book: "#{Book.title}#{phoneNumber.cellPhone}"
        app: "#{app.name}"
        university: "#{university.name}"
        cxlx: "#{sfck.cxlx}"
        id_type: "#{sfck.id_type}"
        org_code: "#{sfck.org_code}"
        bank_code: "#{sfck.bank_code}"
        file: "#{File.extension}"

    gab_wlck_ggxx_mxgx:
        cell_phone: "#{phoneNumber.cellPhone}"
        book: "#{Book.title}"
        app: "#{app.name}"
        university: "#{university.name}"
        cxlx: "#{sfck.cxlx}"
        id_type: "#{sfck.id_type}"
        org_code: "#{sfck.org_code}"
        bank_code: "#{sfck.bank_code}"
        file: "#{File.extension}"

#    gab_wlck_ggxx:
#      sqjgdm: "#{sfck.sqjgdm}"
#      qqdbs: "#{sfck.qqdbs}"
#      ajyt: "#{sfck.ajyt}"
#      mbjgdm: "#{sfck.bank_code}"
#      cxztlb: "#{sfck.cxztlb}"
#      cslx: "#{sfck.cslx}"
#      jjcd: "#{sfck.jjcd}"
#      ajlx: "#{sfck.ajlx}"
#      ajsy: "#{sfck.ajsy}"
#      ajsm: "#{sfck.ajsm}"
#      fsrq: "#{sfck.date}"
#      fssj: "#{sfck.fssj}"
#      qqrxm: "#{Name.name}"
#      qqrzjlx: "#{sfck.police_id_type}"
#      qqrzjhm: "#{sfck.police_id}"
#      qqrdwmc:
#      qqrsjh: "#{phoneNumber.cellPhone}"
#      xcrxm: "#{name.name}"
#      xcrzjlx: "#{sfck.police_id_type}"
#      xcrzjhm: "#{sfck.police_id}"
#
#    gab_wlck_ggxx_mxgx:
 
specific:

    gab_wlck_cgcx:
      cell_phone: "#{phoneNumber.cellPhone}"
      book: "#{Book.title}"
      app: "#{app.name}"
      university: "#{university.name}"
      cxlx: "#{sfck.cxlx}"
      id_type: "#{sfck.id_type}"
      org_code: "#{sfck.org_code}"
      bank_code: "#{sfck.bank_code}"
      file: "#{File.extension}"


    gab_wlck_dtcx:

    gab_wlck_djxj:

    gab_wlck_jjzf:
      cell_phone: "#{phoneNumber.cellPhone}"
      book: "#{Book.title}"
      app: "#{app.name}"
      university: "#{university.name}"
      cxlx: "#{sfck.cxlx}"
      id_type: "#{sfck.id_type}"
      org_code: "#{sfck.org_code}"
      bank_code: "#{sfck.bank_code}"
      file: "#{File.extension}"
